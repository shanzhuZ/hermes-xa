<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.aw.collect.mapper.ThoughtEventMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO hermes_thought_events
            (task_id, seq, event_type, tool_name, content)
        VALUES
            (#{taskId}, #{seq}, #{eventType}, #{toolName}, #{content})
    </insert>

    <select id="selectAfterSeq" resultType="java.util.HashMap">
        SELECT id, task_id, seq, event_type, tool_name, content, created_at
        FROM hermes_thought_events
        WHERE task_id = #{taskId}
          AND seq &gt; #{afterSeq}
        ORDER BY seq ASC
        LIMIT #{limit}
    </select>

    <select id="selectMaxSeq" resultType="java.lang.Integer">
        SELECT MAX(seq)
        FROM hermes_thought_events
        WHERE task_id = #{taskId}
    </select>

    <delete id="deleteByTaskId">
        DELETE FROM hermes_thought_events WHERE task_id = #{taskId}
    </delete>

</mapper>
